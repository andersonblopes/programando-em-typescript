var quantidade = 20;
quantidade = 'Teste'; //Não deve compilar
console.log("" + quantidade);
/*
O tipo pode ser inferido pelo tipo predominante associado(Caso dos arrays);
ou através do contexto em que a varável é utilizada pela primeira vez(Utilização em funções).
*/ 
